require "spec_helper"

describe SpreeSolrFacetFilter do
  it "has a version number" do
    expect(SpreeSolrFacetFilter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
